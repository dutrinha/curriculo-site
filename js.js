function toggleText(triggerSelector, containerSelector) {
    const trigger = document.querySelector(triggerSelector);
    const container = document.querySelector(containerSelector);
  
    trigger.addEventListener('click', () => {
      container.classList.toggle('visible');
      
    });
  }
  
  // Call the function with the appropriate selectors
  toggleText('#trigger-btn', '#text-container',);


  function toggleText(triggerSelector, containerSelector) {
    const trigger = document.querySelector(triggerSelector);
    const container = document.querySelector(containerSelector);
  
    trigger.addEventListener('click', () => {
      container.classList.toggle('visible');
      
    });
  }
  
  // Call the function with the appropriate selectors
  toggleText('#trigger-btn2', '#text-container2',);


  function toggleText(triggerSelector, containerSelector) {
    const trigger = document.querySelector(triggerSelector);
    const container = document.querySelector(containerSelector);
  
    trigger.addEventListener('click', () => {
      container.classList.toggle('visible');
      
    });
  }
  
  // Call the function with the appropriate selectors
  toggleText('#trigger-btn3', '#text-container3',); 